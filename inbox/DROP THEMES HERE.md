Drop your theme `.json` files into this folder, then run from the repo root:

```
node scripts/import.js
```

The script will create a folder in `themes/`, generate a thumbnail, and add your theme to `index.json` automatically.
